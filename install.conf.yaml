- defaults:
    link:
        create: true
        relink: true
#        force: true

- clean: ['~']

- link:
    ~/.dotfiles: ''#

    ~/.profile:
    ~/.bashrc:
    ~/.bash_aliases:

#    ~/.tmux.conf: tmux.conf


    ~/.emacs: emacs

    ~/.gitconfig: gitconfig

- shell:
    - [git submodule update --init --recursive, Installing submodules]
